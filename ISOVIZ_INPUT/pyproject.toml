[project]
name = "isoviz-input"
version = "0.1.0"
description = "Write GD amplitude CSV values into official IsoVIZ .isoviz files and open the visualizer."
readme = "README.md"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
