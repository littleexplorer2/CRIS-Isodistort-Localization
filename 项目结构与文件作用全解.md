# isodistort_back 项目结构与文件作用全解

本文档基于 isodistort_back 当前工程文件清单（以 Git 跟踪文件为准）整理，目标是让你一次性完整理解：
- 项目分层与主流程
- 每个目录的职责边界
- 每个文件的输入、输出、核心作用
- 关键数据文件（isobyu 数据库）与可执行程序用途
- 测试覆盖现状与阅读建议

---

## 1. 项目整体定位

isodistort_back 是一个“离线版 ISODISTORT 工作流封装”项目：
- 底层复用 ISOTROPY 套件中的二进制程序（iso、findsym、smodes、comsubs）。
- 中层用 Python 实现结构读取、位点映射、畸变映射与结构生成。
- 上层提供一站式 API 与交互式入口，便于做从母相到畸变结构的完整计算。

核心价值不是重写群论算法，而是“把群论计算结果落到真实结构坐标上并可导出复现”。

---

## 2. 目录分层与数据流

### 2.1 分层架构

- examples：示例脚本与演示配置。
- isobyu：官方/既有二进制与数据库（算法与群论数据源）。
- isocore/backend：二进制调用封装层（子进程、输入构造、输出解析）。
- isocore/structure：结构层（CIF、坐标变换、对称校验、位点匹配）。
- isocore/distortion：畸变层（路径管理、模式映射、畸变生成、畴生成）。
- isocore/io：输出层（结构导出、结果序列化）。
- isocore/api：业务编排层（把多步骤封装成用户友好的流程）。
- isodistort：对外包名兼容层（从 isocore 重新导出）。
- tests：不依赖/弱依赖二进制的单元测试。

### 2.2 一次计算主链路

1. 读取 CIF（structure.cif_io）
2. 对称性信息获取（structure.symmetry_validator）
3. 子群枚举与模式计算（backend.iso_wrapper）
4. 选择路径（distortion.phase_path）
5. 模式映射到原子位移（distortion.distortion_mapper + structure.site_mapping）
6. 生成畸变结构（distortion.distortion_engine）
7. 导出与持久化（io.structure_exporter / io.result_serializer）

---

## 3. 顶层文件逐个说明

### .gitignore
- 作用：忽略 Python 缓存文件（__pycache__、pyc、pyo）。
- 影响：当前忽略规则非常精简，未覆盖 .venv、output 等目录，意味着这些目录可能被误纳入版本管理，需要团队约定补充。

### README.md
- 作用：项目总说明，包含架构、环境要求、使用方式、模块对应关系。
- 价值：是理解“哪些步骤复用 isobyu、哪些步骤自研”的第一来源。
- 使用建议：先看“12 步骤模块分布”和“核心复用 vs 自研”两段，再看 API 示例。

### main.py
- 作用：交互式向导入口（命令行问答）
- 核心函数：
  - 输入处理：_prompt、_prompt_float、_prompt_int、_prompt_supercell
  - 文件发现：_find_cif_candidates、_choose_cif
  - 业务选择：_choose_distortion_type、_choose_subgroup_idx、_choose_irrep_label、_choose_export_formats
  - 主流程：run_wizard、main
- 适合人群：想不写脚本、直接一步步试跑流程的用户。

### pyproject.toml
- 作用：包构建配置与项目元信息。
- 关键信息：
  - 包名 isodistort，版本 0.1.0
  - Python >= 3.9
  - 运行依赖 numpy/pymatgen/pyyaml
  - 开发依赖 pytest/black/isort/flake8/mypy/pre-commit
  - 包发现 include isodistort* 与 isocore*

### requirements.txt
- 作用：运行依赖清单。
- 说明：额外强调了 spglib（pymatgen 对称分析常用后端），保证对称学相关功能稳定。

### requirements-dev.txt
- 作用：开发、测试、格式化、静态检查与文档依赖。
- 说明：覆盖 pytest、black、isort、flake8、mypy、pre-commit、sphinx。

---

## 4. 配置目录

### config/settings.yaml
- 作用：全局运行配置中心。
- 配置内容：
  - isobyu 路径与可执行文件名（iso/findsym/findsym_cifinput/smodes/comsubs）
  - 默认容差与畸变参数（position_tolerance、default_amplitude、default_supercell）
  - 运行目录（temp_dir、output_dir）与超时（timeout）
- 关键意义：backend 层的真实二进制调用行为几乎都受此文件影响。

---

## 5. 示例目录

### examples/01_basic_workflow.py
- 作用：标准单模式工作流模板。
- 演示：加载结构 -> 子群枚举 -> 选路径 -> 生成畸变 -> 导出文件。
- 价值：最适合“第一次跑通项目”。

### examples/02_mixed_modes.py
- 作用：多模式叠加畸变示例。
- 演示重点：如何传入多个不可约表示贡献系数生成复合畸变。

### examples/03_low_level_api.py
- 作用：底层 API 示例。
- 演示：不通过高层 IsoDistort，直接调用 FindsymWrapper 与 IsoWrapper。
- 价值：适合批处理、二次开发或调试底层文本解析。

### examples/usage_examples.py
- 作用：更接近“文档式示例”的综合脚本。
- 内容：basic 与 mixed 两类调用模式。

### examples/config.yaml
- 作用：批量计算示例配置。
- 内容：k 点、畸变类型、计算方法、输出格式。
- 状态：偏演示性质，不直接驱动主 API（主 API 主要靠代码参数与 settings.yaml）。

---

## 6. isodistort 兼容包

### isodistort/__init__.py
- 作用：公共包名稳定层。
- 机制：通过模块别名映射把 isodistort.* 重定向到 isocore.*。
- 价值：
  - 对外保持 isodistort 作为稳定 API 名称。
  - 内部保留 isocore 作为实现目录，迁移成本更低。

---

## 7. isocore 包：核心实现逐文件全解

## 7.1 包级入口

### isocore/__init__.py
- 作用：项目核心包说明与版本号。
- 内容：模块分层文档字符串 + __version__。

### isocore/api/__init__.py
- 作用：导出 IsoDistort，简化导入路径。

### isocore/backend/__init__.py
- 作用：统一导出 backend 层主要类与数据类。

### isocore/distortion/__init__.py
- 作用：统一导出畸变层核心类。

### isocore/io/__init__.py
- 作用：统一导出导出器与序列化器。

### isocore/structure/__init__.py
- 作用：统一导出结构层 API。
- 价值：上层调用只需从 structure 包导入，无需关心子模块路径。

### isocore/utils/__init__.py
- 作用：统一导出配置、异常、文本解析工具。

## 7.2 API 编排层

### isocore/api/core_api.py
- 核心类：IsoDistort
- 作用：把底层能力编排成“可用工作流接口”。
- 内部组件：
  - backend：FindsymWrapper、IsoWrapper
  - structure：SymmetryValidator、SiteMapper
  - distortion：PhasePath、DistortionMapper、DistortionEngine、DomainGenerator
  - io：StructureExporter、ResultSerializer
- 关键状态：structure、symmetry_info、subgroups、phase_path、distortion_modes、distorted_structure。
- 关键方法：
  - load_structure / set_structure
  - list_subgroups
  - select_path
  - generate_distortion
  - generate_mixed_distortion
  - export / save_result / load_result / generate_domains
- 使用意义：这是绝大多数用户应该直接调用的主入口。

## 7.3 backend 二进制封装层

### isocore/backend/base_wrapper.py
- 核心类：BaseWrapper
- 作用：所有二进制调用的公共基础设施。
- 职责：
  - 管理临时输入输出
  - 统一子进程调用与超时
  - 注入环境变量（例如 ISODATA）
  - 统一错误抛出（WrapperRunError、WrapperTimeoutError）
- 价值：屏蔽平台差异与调用细节，减少重复代码。

### isocore/backend/findsym_wrapper.py
- 数据类：FindsymResult
- 核心类：FindsymWrapper
- 作用：把晶格参数与原子坐标送入 findsym，解析空间群与 Wyckoff 信息。
- 关键能力：
  - identify：组织输入并执行二进制
  - _build_input：生成关键字输入格式
  - _parse_output：解析空间群号、符号、位点、晶格参数
- 输出对象：包含 space_group_number、space_group_symbol、wyckoff_sites、lattice_params、raw_output。

### isocore/backend/iso_wrapper.py
- 数据类：SubgroupInfo、DistortionMode、IsoResult
- 核心类：IsoWrapper
- 作用：封装 iso 程序四类核心能力。
- 主要接口：
  - list_subgroups：按母群和畸变类型枚举子群
  - calc_distortion_modes：计算模式基矢（不可约表示）
  - get_site_splitting：Wyckoff 分裂关系
  - get_domain_operations：畴相关对称操作
- 工程意义：这是“群论计算引擎”在 Python 中的主要门面。

## 7.4 structure 结构层

### isocore/structure/cif_io.py
- 作用：结构输入与基础提取。
- 函数：
  - read_cif：读取 CIF 到 Structure
  - structure_from_params：由晶格与坐标直接构建 Structure
  - get_lattice_params：提取 6 晶格参数
  - get_atom_info：提取原子种类与分数坐标
- 价值：连接“文本/文件结构”与“计算对象”的第一步。

### isocore/structure/coordinate_transform.py
- 作用：坐标与晶格变换底层工具。
- 函数：
  - frac_to_cart / cart_to_frac
  - apply_affine_transform
  - wrap_to_unit_cell
  - build_supercell
  - transform_lattice
  - coordinates_are_equal
- 价值：畸变映射和畴变体生成都依赖这里，属于几何基础层。

### isocore/structure/site_mapping.py
- 核心类：SiteMapper
- 作用：把理论位点信息与真实结构原子索引对齐。
- 背景：iso 给的是理论分裂关系，真实结构要做坐标容差匹配。
- 价值：是“群论结果落地坐标”的关键桥梁之一。

### isocore/structure/symmetry_validator.py
- 核心类：SymmetryValidator
- 作用：对结构做对称信息校验并输出统一摘要。
- 当前行为：
  - 通过 SpacegroupAnalyzer 获取空间群号与符号
  - 构建 Wyckoff 分组与等效原子索引
  - 检测占位无序（site.is_ordered）
  - 返回 has_disorder 与 tolerance
- 工程定位：作为结构质量与对称摘要入口，给后续路径选择与映射提供基础元数据。

## 7.5 distortion 畸变层

### isocore/distortion/phase_path.py
- 核心类：PhasePath
- 作用：描述并校验一条相变路径（母群 -> 子群，畸变类型等）。
- 价值：把“用户选择”转成“可执行参数包”。

### isocore/distortion/distortion_mapper.py
- 核心类：DistortionMapper
- 作用：将 iso 的抽象模式向量映射为每个原子的位移向量。
- 难点：需要结合位点匹配与坐标变换处理等效原子分配。
- 项目地位：全项目最关键的自研逻辑之一，决定结果和在线版一致性。

### isocore/distortion/distortion_engine.py
- 核心类：DistortionEngine
- 作用：按幅度放缩位移，应用单模式或多模式，生成新结构。
- 关键能力：
  - generate_single_mode
  - generate_mixed_mode
  - 可选 supercell 扩胞后再应用位移
- 输出：畸变后的 pymatgen Structure。

### isocore/distortion/domain_generator.py
- 核心类：DomainGenerator
- 作用：结合 iso 给出的对称操作，生成各个畴变体结构。
- 价值：支持多畴态分析与结构比较。

## 7.6 io 输出层

### isocore/io/structure_exporter.py
- 核心类：StructureExporter
- 作用：导出结构文件（CIF、POSCAR、XYZ）。
- 价值：把计算结果接入 VESTA/VASP 等外部工具链。

### isocore/io/result_serializer.py
- 核心类：ResultSerializer
- 作用：将中间结果与最终结果序列化为 JSON。
- 细节：处理 numpy 与数据类对象，支持后续复算和审计。

## 7.7 utils 工具层

### isocore/utils/config_loader.py
- 核心类：Config
- 作用：读取 settings.yaml，构建全局配置单例，初始化环境变量。
- 函数：get_config
- 价值：确保路径、超时、容差在全项目一致。

### isocore/utils/exceptions.py
- 作用：统一异常体系。
- 结构：
  - 根异常 IsodistortError
  - wrapper 异常（运行失败、超时、解析失败）
  - structure 异常（位点匹配、坐标变换）
  - distortion 异常（模式错误、路径错误）
- 价值：让上层能按错误类别做恢复或提示。

### isocore/utils/text_parser.py
- 作用：解析 iso/findsym 的文本输出。
- 函数：
  - extract_section
  - parse_floats
  - parse_space_group_number
  - parse_wyckoff_sites
  - parse_subgroup_list
- 价值：二进制工具输出不是结构化数据，该文件是“文本到对象”关键模块。

---

## 8. tests 目录逐文件说明

### tests/__init__.py
- 作用：将 tests 目录标记为包。

### tests/test_config.py
- 作用：测试配置加载与环境变量设置。
- 检查点：可执行路径解析、临时目录存在、ISODATA 环境变量注入。

### tests/test_distortion.py
- 作用：测试畸变引擎的纯 Python 逻辑。
- 覆盖：单模式位移、扩胞情况、多模式混合。
- 价值：不依赖外部二进制，能快速回归核心自研逻辑。

### tests/test_structure.py
- 作用：测试结构层基础工具与对称摘要。
- 覆盖：
  - 坐标包裹与坐标比较
  - 超胞构建
  - 对称性摘要输出（空间群、Wyckoff 数量）
  - 占位无序识别（混占/部分占位场景）

---

## 9. isobyu 目录：二进制与数据库逐文件说明

这一层是项目“计算内核依赖”。一般视为只读资源，不建议修改。

### 9.1 可执行程序（无扩展名）

### isobyu/iso
- 文件类型：二进制可执行文件。
- 作用：ISOTROPY 主程序，负责子群枚举、模式计算、位点分裂、畴操作等群论计算。
- 在项目中的调用者：isocore/backend/iso_wrapper.py。

### isobyu/findsym
- 文件类型：二进制可执行文件。
- 作用：根据晶格与原子位置识别空间群与 Wyckoff 信息。
- 调用者：isocore/backend/findsym_wrapper.py。

### isobyu/findsym_cifinput
- 文件类型：二进制可执行文件。
- 作用：把 CIF 转为 findsym 可读输入，通常用于桥接流程。
- 调用场景：直接命令行调用时常见；本项目当前以 Python 读取结构为主。

### isobyu/smodes
- 文件类型：二进制可执行文件。
- 作用：位移模式相关计算工具（与不可约表示模式分解有关）。
- 在当前项目中的状态：预留支持，主要流程核心仍在 iso_wrapper。

### isobyu/comsubs
- 文件类型：二进制可执行文件。
- 作用：重构相变路径与公共子群搜索工具。
- 在当前项目中的状态：提供扩展能力，主流程未深度编排。

### 9.2 官方说明文档与样例

### isobyu/findsym.txt
- 作用：findsym 使用手册。
- 关键内容：关键字输入格式、容差定义、输出结构、与 findsym_cifinput 的配合方式。

### isobyu/smodes.txt
- 作用：smodes 使用手册。
- 关键内容：输入行格式、k 点与模式输出解释、示例说明。

### isobyu/comsubs.txt
- 作用：comsubs 使用手册。
- 关键内容：重构相变输入定义、应变/邻近距离/shuffle 约束含义。

### isobyu/findsym_sample.in
- 作用：findsym 示例输入（NaCl）。
- 价值：可直接作为调试模板对照 Python 输入构造。

### isobyu/findsym_sample.log
- 作用：findsym 示例输出。
- 价值：最重要的解析对照样本，可用于验证 parse_space_group_number 与 parse_wyckoff_sites。

### isobyu/smodes_sample.in
- 作用：smodes 示例输入。

### isobyu/smodes_sample.out
- 作用：smodes 示例输出。
- 价值：理解模式输出字段及位移向量格式。

### isobyu/comsubs_sample.in
- 作用：comsubs 示例输入（NaCl -> CsCl）。

### isobyu/comsubs_sample.log
- 作用：comsubs 示例输出。
- 价值：理解公共子群与路径筛选结果结构。

### 9.3 数据库文件（data_*.txt 与 const.dat）

这些是 iso/findsym 运行时数据库，体积大、结构专用、用于群论与对称学查表。

### isobyu/const.dat
- 作用：常数表（数值到符号表达映射），用于输出与内部计算常量引用。

### isobyu/data_diperiodic.txt
- 作用：二维/二周期群相关标签库（diperiodic groups）。

### isobyu/data_images.txt
- 作用：图像/构型标识数据库（image labels），用于群作用下对象标记。

### isobyu/data_irreps.txt
- 作用：不可约表示标签数据库（如 GM1、X1、R1+ 等）。
- 价值：模式命名和对称表示解释的基础字典。

### isobyu/data_isotropy.txt
- 作用：isotropy 子群关系与索引表。
- 价值：子群枚举与关联查询的核心底表之一。

### isobyu/data_little.txt
- 作用：little group 与 k 点标签、计数信息。
- 价值：k 空间相关群论计算的重要查表。

### isobyu/data_magnetic.txt
- 作用：磁空间群标签与索引库。
- 价值：磁畸变/磁群分析时的核心数据库。

### isobyu/data_space.txt
- 作用：空间群点操作标签数据库（旋转、反演、螺旋等）。

### isobyu/data_ssg.txt
- 作用：超空间群（SSG）相关索引数据库。
- 体量：很大，属于高维/调制结构相关支撑数据。

### isobyu/data_ssgmag.txt
- 作用：磁性超空间群相关索引数据库。
- 体量：全仓最大级别文件之一，磁性扩展场景关键。

### isobyu/data_wyckoff.txt
- 作用：Wyckoff 位点统计/索引数据库。
- 价值：位点分裂与位点标注计算的底层数据源。

---

## 10. 当前目录中常见但不属于核心源码的目录

### .venv
- 作用：本地 Python 虚拟环境。
- 建议：不应作为项目结构理解对象，也不应纳入版本控制。

### .pytest_cache
- 作用：pytest 缓存。
- 建议：可删除、可重建，不属于业务逻辑。

### output
- 作用：运行产物目录（导出结构、临时文件等）。
- 建议：保留目录结构，但产物可清理。

### isodistort.egg-info
- 作用：安装/打包元信息目录。
- 建议：由构建过程自动生成，不建议手改。

---

## 11. 你应该如何阅读这个项目（最快上手路径）

1. 先读 README.md（定位功能边界与阶段映射）
2. 再读 isocore/api/core_api.py（掌握高层主流程）
3. 然后读 backend 两个 wrapper（理解与二进制的边界）
4. 再读 distortion_mapper.py 与 site_mapping.py（核心自研难点）
5. 最后看 tests 与 examples（验证理解是否正确）

---

## 12. 对“项目完整性”的一句话总结

isodistort_back 的工程设计是清晰且可扩展的：
- 群论计算依赖成熟外部内核（isobyu）
- 核心创新放在结构映射与工作流编排
- 测试已覆盖基础流程，但高级交叉校验与更多边界场景仍有扩展空间

这也是它最适合科研开发的形态：底层可靠、上层可控、可逐步逼近与在线 ISODISTORT 的一致性。
