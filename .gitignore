# environment
/.venv/
/.vscode/
/CRIS.code-workspace

# logs (repo-wide caches if invoked from the root)
*.log
/.pytest_cache/
/.ruff_cache/
/.test-tmp*/

# data (read-only archives, not subproject source)
/experiment_data/
/webpage_info/
/output_compare/

# shortcuts kept at the repo root for convenience
ISOViz.lnk
VESTA.lnk

# not part of this git remote
/GD/
